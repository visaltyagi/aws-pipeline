version: 0.2
phases:
 install:
   commands:
     - sudo yum update 
     - sudo yum install php7.4 -y 
 build:
   commands:
     - echo Build started on `date`
     - zip hello.zip ./index.php
artifacts:
  type: 
  files:
    - index.php
    - appspec.yml
    - scripts/install_dependencies
    - scripts/start_server
    - aws.gif
    - back.jpg
